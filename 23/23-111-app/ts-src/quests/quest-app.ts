import { ef } from "../meh/index.js";

namespace GUI
{
	const { main, nav, article, section, div, h1, h2, h3, h4, p, span, input, textarea, button, ul, li } = ef;

	export const App = () =>
	{
		return main(  );
	};
}


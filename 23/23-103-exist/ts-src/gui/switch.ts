import { Select, ef } from "../meh/index.js";
const log = console.log;
const { div, span, ul, li } = ef;

//  //

export const Tabs = () =>
{
	return ul( { class: "tabs" },
	);
}

export const Switch = (  ) =>
{
	;
}


import { Exist, Leafr, Leaf, Renn, Branch, defs, log, root, ef } from "../meh/index.js";

type create = () => { title : string, content : defs.Node };

export const Page = ( create : create ) =>
{
	const def = create();
}
